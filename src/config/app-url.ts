const BASE_URL = 'http://localhost:3000'

export const APP_URL = {
	PRODUCTS: {
		ACTIONS: `${BASE_URL}/products/actions`,
		NEW: `${BASE_URL}/products/new`
	},
	USERS: {
		PURCHASES: `${BASE_URL}/users/purchases`
	},
	ARTICLES: {
		HOME: `${BASE_URL}/articles`
	}
}
